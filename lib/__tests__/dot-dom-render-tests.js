const {H} = require('dot-dom');
const render = require('../dot-dom-render');

describe('.dom render', function () {

  describe('#render', function () {
    it('should correctly render empty document', function() {

    })
  })

})
